import React from "react";
import { apis } from "../lib/axios";

const Mypage = () => {
  React.useEffect(() => {}, []);
  return (
    <React.Fragment>
      <div className="CommonPageStyle">mypage</div>
    </React.Fragment>
  );
};

export default Mypage;
